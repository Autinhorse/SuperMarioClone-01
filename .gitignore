# Godot 4+ specific ignores
.godot/
.import/

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# System/tool files
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/

# External project sitting in the worktree but managed independently
# (its own .git, its own remote — not a submodule, not synced via this
# repo).
agent-sprite-forge/

# Export output
/export/
/build/

# Prevent accidental root-level web exports (we export to /docs/)
/index.html
/index.js
/index.wasm
/index.pck
/index.png
/index.icon.png
/index.apple-touch-icon.png
/index.audio.worklet.js
/index.audio.position.worklet.js
/index.*.import
